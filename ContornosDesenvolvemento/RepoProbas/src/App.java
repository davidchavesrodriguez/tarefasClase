import static org.junit.Assert.assertEquals;

import java.util.List;

import org.junit.Test;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

public class App {

  public static void main(String[] args) {
    // System.setProperty("webdriver.chrome.driver", "driver/chromedriver");
    // WebDriver driver = new ChromeDriver();
    /*
     * try {
     * 
     * 
     * 
     * 
     * WebElement cajaBusqueda = driver.findElement(By.id("searchInput"));
     * cajaBusqueda.sendKeys("Taragoña");
     * WebElement botonBusqueda = driver.findElement(By.id("searchButton"));
     * botonBusqueda.click();
     * 
     * assertEquals("Taragoña - Wikipedia, la enciclopedia libre",driver.getTitle())
     * ;
     * 
     * //System.out.println(driver.getTitle());
     * driver.findElement(By.id("n-portal")).click();
     * System.out.println(driver.findElement(By.id("fristHeading")));
     * System.out.println(tituloPagina.getTitle());
     * assertEquals("Portal:Comunidad", cajaBusqueda);
     * 
     * System.out.println(driver.findElement(By.id("footer")).getAttribute("role"));
     * 
     * List elementosDiv = driver.findElements(By.tagName("div"));
     * for (WebElement webElement : elementosDiv) {
     * System.out.println(webElement.getAttribute("innerText"));
     * }
     * Thread.sleep(3000);
     * } catch (InterruptedException e) {
     * // TODO Auto-generated catch block
     * e.printStackTrace();
     * }
     * 
     * driver.quit();
     */

  }

}